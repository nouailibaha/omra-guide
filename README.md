# omra-guide

PWA APP.

# Local run

```bash
python3 -m http.server 8000
```

Node.js (npx) :

```bash
npx serve
```

PHP :

```bash
php -S localhost:8000
```

3. _Open in the browser_

   ```
   http://localhost:8000
   ```
